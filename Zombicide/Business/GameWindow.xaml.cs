using System.Windows;

namespace Zombicide
{
    /// <summary>
    /// Logique d'interaction pour GameWindow.xaml
    /// </summary>
    public partial class GameWindow : Window
    {
        public GameWindow()
        {
            //InitializeComponent();
            //ZombicideMapFile map = new ZombicideMapFile(Zombicide.Properties.Resources.map1, "map1");
            ////ZombicideMapFile map = new ZombicideMapFile("C:\\Users\\LandryC\\Documents\\Visual Studio 2015\\Projects\\ExoWPF\\ZombicideOpenMap\\ZombicideOpenMap\\map1.txt");
            //map.ReadFile();
            //var a = new FattyToxic(new Location("1"));

            //RouteEngine routeEngine = new RouteEngine(map.Connections, map.Locations);
            //Location startLocation = null;
            //Location endLocation = null;
            //int startLocationNumber = 0;
            //int endLocationNumber = 48;
            //foreach (var loc in map.Locations)
            //{
            //    if (loc.Identifier.Equals(startLocationNumber.ToString()))
            //    {
            //        startLocation = loc;
            //    }
            //    if (loc.Identifier.Equals(endLocationNumber.ToString()))
            //    {
            //        endLocation = loc;
            //    }


            //}

            //var result = routeEngine.CalculateMinCost(startLocation);

            //var test = result[endLocation].Connections;
        }
    }
}
